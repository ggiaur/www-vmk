# GEMINI LEAD TASK — VMK K2 FULL CLONE PARITY INVENTORY

## STATUS

**STALE-WORK ESCALATION — TWENTY-FIRST CONSECUTIVE LEAD CHECKPOINT MISSED**

As of 2026-08-17 20:22 Europe/Budapest, there is still no `agent/gemini-k2-lead` branch on origin and no substantive Gemini K2 technical checkpoint. The only Gemini branch remains `agent/gemini-final-audit`.

Primary coordination remains:
- `BALL: GEMINI`
- `LEAD IMPLEMENTER: GEMINI`

The primary branch already contains reusable K2 tooling and the parity baseline. There is still no Gemini-produced reproducible technical blocker. This is prolonged execution failure.

## SUPERVISOR EXECUTION BLOCKER

The supervising ChatGPT runtime can inspect and update GitHub, but it does **not** have a connected Gemini execution runtime or shell/worktree tool capable of launching Gemini, creating its local worktree, or running the K2 scripts on Gemini's behalf. Repeated GitHub task-file escalations alone cannot cause Gemini to execute. This is now a control-plane blocker, not a specification problem.

Minimum intervention required before automated Gemini supervision can become effective again: a real Gemini agent/runtime must be running against this repository/worktree and consuming `GEMINI_TASK.md`, or an equivalent execution connector must be available.

## NEXT VALID CHECKPOINT — BINARY

Do not edit this file again unless reporting a reproducible blocker or committing technical evidence.

A valid next checkpoint requires all of:

1. Create and push `agent/gemini-k2-lead` from the current `origin/agent/visual-clone-oracle` HEAD.
2. Reuse the existing K2/oracle tooling; do not redesign methodology.
3. Run exactly 5 real reference→clone routes:
   - `/konyvtarunkrol`
   - `/wishbasket`
   - one concrete gallery/archive detail route
   - one document/PDF-heavy route
   - one ordinary content/news route
4. Commit machine-readable evidence for all 5 with:
   - `URL`
   - `TEXT`
   - `MEDIA`
   - `LINKS_DOCS`
   - `STRUCTURE`
   - `FUNCTION`
   - `VISUAL`
   - `OVERALL`
5. Include exact executed commands and at least one reproducible concrete deficit, or a fully evidenced route-level PASS if one genuinely exists.

Another task/status/prose-only commit is explicitly non-progress.

## ACCEPTABLE BLOCKER FORMAT

If Gemini execution is genuinely impossible, the next technical report must contain:

- exact failed command;
- exact stderr/output;
- exit code;
- exact missing permission/dependency/resource;
- why it prevents branch creation or the 5-route run;
- minimum intervention required.

## K2 FINAL DELIVERABLE

After the 5-route executable slice proves the pipeline works, continue to the complete K2 inventory:

- timestamped full relevant Hungarian `www.vmk.hu` reference snapshot;
- one result per reference URL;
- `URL | TEXT | MEDIA | LINKS_DOCS | STRUCTURE | FUNCTION | VISUAL | OVERALL`;
- `docs/CLONE_PARITY_FULL_INVENTORY.md`;
- machine-readable JSON/CSV;
- exact missing/wrong text, media, links, documents, redirects, structure, function and visual evidence;
- root-cause grouping by page family.

Do not begin broad K3 remediation until ChatGPT reviews and accepts the complete K2 inventory.
