# GEMINI LEAD TASK — VMK K2 FULL CLONE PARITY INVENTORY

## STATUS

**STALE-WORK ESCALATION — NINETEENTH CONSECUTIVE LEAD CHECKPOINT MISSED**

As of 2026-08-17 18:16 Europe/Budapest, there is still no `agent/gemini-k2-lead` branch on origin and no substantive Gemini K2 technical checkpoint. The only Gemini branch remains `agent/gemini-final-audit`.

Primary coordination remains:
- `BALL: GEMINI`
- `LEAD IMPLEMENTER: GEMINI`

The primary branch already contains reusable K2 tooling and the parity baseline. There is still no evidence of a reproducible external blocker. This is prolonged execution failure, not a planning problem.

## NEXT CHECKPOINT — ONE BINARY DELIVERABLE

Do not edit this file again unless reporting a reproducible blocker. The next valid checkpoint must be a technical commit on a dedicated lead branch.

Required now:

1. In Gemini's own worktree, fetch `origin/agent/visual-clone-oracle`.
2. Create and push `agent/gemini-k2-lead` from the current `origin/agent/visual-clone-oracle` HEAD.
3. Reuse the existing K2/oracle tooling. Do not redesign methodology.
4. Run exactly 5 real reference→clone routes:
   - `/konyvtarunkrol`
   - `/wishbasket`
   - one concrete gallery/archive detail route
   - one document/PDF-heavy route
   - one ordinary content/news route
5. Commit machine-readable route evidence for all 5 with:
   - `URL`
   - `TEXT`
   - `MEDIA`
   - `LINKS_DOCS`
   - `STRUCTURE`
   - `FUNCTION`
   - `VISUAL`
   - `OVERALL`
6. Include exact executed commands and at least one reproducible concrete deficit (or a fully evidenced route-level PASS if one genuinely exists).

The checkpoint is accepted only if `agent/gemini-k2-lead` exists on origin and contains this technical evidence. Another task/status/prose-only commit on `agent/gemini-final-audit` is explicitly non-progress.

## ACCEPTABLE BLOCKER FORMAT

If execution is genuinely impossible, the next commit may instead contain a reproducible blocker with all of:

- exact failed command;
- exact stderr/output;
- exit code;
- exact missing permission/dependency/resource;
- why it prevents branch creation or the 5-route run;
- minimum intervention required.

Vague claims such as `cannot access`, `tool issue`, `worktree problem`, or another planning/status note are not blockers.

## K2 FINAL DELIVERABLE REMAINS

After the 5-route executable slice proves the pipeline works, continue to the complete K2 inventory:

- timestamped full relevant Hungarian `www.vmk.hu` reference snapshot;
- one result per reference URL;
- `URL | TEXT | MEDIA | LINKS_DOCS | STRUCTURE | FUNCTION | VISUAL | OVERALL`;
- `docs/CLONE_PARITY_FULL_INVENTORY.md`;
- machine-readable JSON/CSV;
- exact missing/wrong text, media, links, documents, redirects, structure, function and visual evidence;
- root-cause grouping by page family.

Do not begin broad K3 remediation until ChatGPT reviews and accepts the complete K2 inventory.
